#include <iostream>

using namespace std;
struct Date{
    int day{};
    int month{};
    int year{};
};

void printDate(const Date& date){
    cout<<date.day<<" "<<date.month<<" "<<date.year;
}

int main()
{
    Date date{1, 2, 1990};
    printDate(date);
    return 0;
}
